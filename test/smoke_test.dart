import 'package:flutter_test/flutter_test.dart';

import 'package:dose_certa/app.dart';

void main() {
  test('DoseCertaApp can be constructed', () {
    const app = DoseCertaApp();
    expect(app, isA<DoseCertaApp>());
  });
}
